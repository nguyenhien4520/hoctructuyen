<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ChatGPT Bài Tập</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>

<body>
    {{>header}}
    {{!-- <h1>{{user.classes.name}}</h1> --}}
    <div class="d-flex mt-2">
        <h5 class="ml-auto"><i class="fa fa-user" aria-hidden="true"></i> {{user.name}}</h5>
    </div>
    <div class="container mt-2">
        <div class="row">
            <div class="col-3">
                {{#each user.classes}}
                <div class="card mb-3">
                    <div class="card-header">
                        <b>{{this.name}}</b>
                    </div>
                    <div class="card-body" style="padding: 0;">
                        {{#if this.tests.length}}
                        <ul class="list-group list-group-flush">
                            {{#each this.tests}}
                            <li class="list-group-item">
                                <a href="student/test/{{this._id}}">{{this.title}}</a>
                            </li>
                            {{/each}}
                        </ul>
                        {{else}}
                        <p>Không có bài kiểm tra nào</p>
                        {{/if}}
                    </div>
                </div>
                {{/each}}

            </div>


            <div class="col-9">
                <section id="hero">
                </section>

                <section id="features">
                    <div class="container">
                        <h2>Tính năng nổi bật</h2>
                        <div class="d-flex justify-content-between">
                            <div class="feature-box">
                                <i class="fas fa-brain"></i>
                                <h3>AI thông minh</h3>
                                <p>Tự động tạo các bài tập dựa trên kiến thức và kỹ năng của học sinh.</p>
                            </div>
                            <div class="feature-box">
                                <i class="fas fa-user-friends"></i>
                                <h3>Dễ dàng sử dụng</h3>
                                <p>Giao diện thân thiện với người dùng.</p>
                            </div>
                            <div class="feature-box">
                                <i class="fas fa-chart-line"></i>
                                <h3>Theo dõi tiến độ</h3>
                                <p>Giáo viên có thể theo dõi tiến độ học tập của học sinh theo thời gian thực.</p>
                            </div>
                            <div class="feature-box">
                                <i class="fas fa-database"></i>
                                <h3>Ngân hàng câu hỏi</h3>
                                <p>Lưu trữ và quản lý câu hỏi để sử dụng lại khi cần thiết.</p>
                            </div>
                        </div>
                    </div>
                </section>
            </div>
        </div>
    </div>

    {{>footer}}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
